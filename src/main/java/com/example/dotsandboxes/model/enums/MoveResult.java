package com.example.dotsandboxes.model.enums;

/**
 * enum that is used to determine if a move have caused the game to end,
 * otherwise the move is valid
 */
public enum MoveResult {
    valid,gameOver
}
